using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Steak : Foodd


{

    // Start is called before the first frame update
    void Start()
    {
        Value = GameConstants.SteakValue;

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
