using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Steak : Food
{
    // Start is called before the first frame update
    void Start()
    {
        PickValue = GameConstants.SteakValue; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
